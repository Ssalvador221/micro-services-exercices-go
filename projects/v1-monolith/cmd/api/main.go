package main

func main() {
	// TODO: CREATE HANDLER, SERVE, ROUTE GROUPS, OS.GETENV.
	/* 	ctx := context.Background()
	   	db, _ := sql.Open("mysql", dsn)
	   	queries := sqlc.New(db)

	   	repo := repository.NewUserRepository(ctx, queries)
	   	uc := application.CreateUserUseCase(repo) */
}
